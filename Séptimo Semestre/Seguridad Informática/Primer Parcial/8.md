DDos ocurre spammeando flags (tcp pings) y logrando half open connections.

Hay que descargar snore y definir reglas para ids.

- Vulnerability attack:
	- Send a few crafted messages to target app vulnerability.
	- Malicious messages called exploit
	- Remotely stopping or crashing services
- Connection flooding:
	- Overwhelming connection queue TCP SYN flood.
- Bandwidth flooding attack:
	- Overwhelming comms link with packets.
	- Strength in flooding attack lies in volume rather than content.

DoS:

- Source of attack small # of nodes.
- Source IP typically spoofed.

DDoS:

- From thousands of nodes
- IP addresses often not spoofed
- DDoS network tools: Scapy, hping3, DDoSIM, RUDY, etc.

```sex
http, https
	↓
TL  TCP  ODP → IDS | Firewall (Acc, ) 
			 → Encryption (Nonrep, Auth, Conf, Ava, Int)

H_T | DATA

SP, DP
Flags: SYN, ACK, FIN

```

Threeway handshake process (incluir esquema)

Debe colocarse un buen IDs entre el internet y la víctima. 

Para defenderse de un DoS pueden usarse proxy servers, redundancia y ancho de banda (overprovisioning).

## Symetric Syphers

Conventional / secret / single key. Sender and recipient share a common key.

All classical enc. alg. are secret-key. Prior to public-key in 1970s.


Block ciphers (blocks of n bits) / stream ciphers (letter by letter, bit by bit).



